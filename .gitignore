# Dependencies
node_modules/

# Next.js
website/.next/
website/out/

# Environment
.env
.env.local
.env.production

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude
.claude/
.vercel

# DB snapshots — full institutional dumps from
# scripts/export-turso-snapshot.ts. Generate locally; do not commit.
# See system/MIGRATION-CLOUDFLARE-D1.md.
system/snapshots/

# Collection dataset: the generated tables and the packaged deposit are
# reproducible from export-dataset.ts, so they are not tracked. The authored
# parts — the data dictionary and the deposit metadata — are.
dataset/*.csv
dataset/*.jsonl
dataset/MANIFEST.json
mna-collection-dataset-*.zip

# Provider cooldown state — local, ephemeral, never shared
system/.llm-cooldowns.json
