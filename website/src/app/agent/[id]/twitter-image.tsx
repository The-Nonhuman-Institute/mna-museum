/**
 * Twitter/X card image for /agent/[id] — re-exports the OG route so
 * Twitter and OpenGraph share the same card.
 */
export { default, runtime, alt, size, contentType } from "./opengraph-image";
